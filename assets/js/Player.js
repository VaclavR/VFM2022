export class Player {
    constructor(id, name, position, number, teamId) {
      this.id = id;
      this.name = name;
      this.position = position;
      this.number = number;
      this.teamId = teamId;
    }
  
    getTeam() {
      // Metoda pro získání týmu, ke kterému hráč patří
    }
  
    // Další metody pro práci s hráčem
  }